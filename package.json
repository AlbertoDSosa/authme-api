{
  "name": "authme-api",
  "version": "0.1.2",
  "description": "Custom user authentication for Node.",
  "main": "server/",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "export NODE_ENV=production || NODE_ENV= production || SET \"NODE_ENV=production\" && node server/",
    "dev": "export NODE_ENV=development || NODE_ENV=development || SET \"NODE_ENV=development\" && nodemon server/"
  },
  "keywords": [
    "authentication"
  ],
  "author": "Alberto D.Sosa <albertodsosa@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^3.0.5",
    "express": "^4.16.4",
    "uniqid": "^5.0.3",
    "validator": "^10.11.0"
  },
  "devDependencies": {
    "casual": "^1.6.0",
    "nodemon": "^1.18.10"
  }
}
